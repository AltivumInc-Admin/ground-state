# Feature Optimization

> This file tracks ways to REFINE features that already exist in this project.
> It is NOT a roadmap of new features to add — nothing here introduces new
> functionality. Every item makes an existing feature cleaner, faster, safer,
> more accessible, or otherwise better.
>
> Maintained by the `/optimize-features` command. Last full inventory: 2026-07-02

## Feature Inventory

### A. Marketing & content pages

| # | Feature | What it does | Where it lives | Last reviewed |
|---|---------|--------------|----------------|---------------|
| 1 | Landing page scroll narrative | The homepage: a five-section scroll story (01 Hero · 02 Problem · 03 Proof · 04 Inside · 05 Join) with stat counters, responsive comparison/matrix tables, ROI math, and the wave–particle figure divider. | `src/pages/Landing.jsx`, `src/sections/{Hero,Problem,Proof,Inside,FinalCta}.jsx` | 2026-07-02 |
| 2 | The Story page | Founder/origin E-E-A-T narrative at `/story`, prerendered to static HTML with its own head/canonical for crawlers. | `src/pages/Story.jsx`, `src/sections/Story.jsx` | 2026-07-02 |
| 3 | The Signal web archive | Public newsletter archive: issue index (`/signal`) and per-issue reader (`/signal/:slug`) rendering Sanity Portable Text, each prerendered with article meta. | `src/pages/Signal.jsx`, `src/pages/SignalIssue.jsx`, `src/components/IssueBody.jsx`, `src/lib/issues.js` | 2026-07-02 |

### B. Conversion & membership flows

| # | Feature | What it does | Where it lives | Last reviewed |
|---|---------|--------------|----------------|---------------|
| 4 | Membership application (`/apply`) | Multi-field gated application (name/email/company/role/type/stage/modality/want) with client validation, per-field accessible errors, honeypot, shared `useIntakeSubmit` state machine, and an honest no-endpoint preview. | `src/pages/Apply.jsx`, `src/lib/useIntakeSubmit.js` | 2026-07-02 |
| 5 | The Signal signup + double opt-in | Free-tier email capture (landing CTA, archive, issue pages) with honeypot + concurrent-submit guard, and the magic-link confirmation landing (`/confirm`). | `src/components/SignalSubscribe.jsx`, `src/pages/Confirm.jsx`, `src/lib/submit.js` | 2026-07-02 |
| 6 | Membership activation & Stripe Checkout flow | `/activate` plan choice → Stripe Checkout redirect (open-redirect guarded), `/welcome` verifies the session against the backend before claiming membership active; bfcache-safe. | `src/pages/Activate.jsx`, `src/pages/Welcome.jsx` | 2026-07-02 |

### C. Immersive & visual features

| # | Feature | What it does | Where it lives | Last reviewed |
|---|---------|--------------|----------------|---------------|
| 7 | Hero ground-state 3D scene | R3F harmonic-well particle simulation (relaxation to gaussian |ψ₀|², zero-point breathing, scroll-injected energy); lazy chunk, DPR clamp, in-view frameloop gating, WebGL error boundary, reduced-motion static frame. | `src/three/GroundStateScene.jsx`, `src/components/HeroScene.jsx` | 2026-07-02 |
| 8 | Physics SVG figures | Scroll-drawn line-art diagrams (wave–particle, energy levels, Bloch sphere) used as section dividers/illustrations, with captions and reduced-motion fallbacks. | `src/components/figures/*.jsx`, `src/three/BlochScene.jsx` | 2026-07-02 |

### D. Backend services

| # | Feature | What it does | Where it lives | Last reviewed |
|---|---------|--------------|----------------|---------------|
| 9 | Stripe Checkout Lambda | Dependency-free Lambda (HTTP API): `POST /checkout` (plan→session), `GET /session` (safe status subset), `POST /webhook` (HMAC-verified, DynamoDB-idempotent); secrets from Secrets Manager, CloudWatch alarms. | `backend/checkout/src/handler.mjs`, `backend/checkout/template.yaml` | 2026-07-02 |
| 10 | Subscribe / verify Lambda + suppression | Double opt-in `POST /subscribe` (honeypot, source allowlist, hashed token, consent IP) + `POST /verify` (single-use token → session JWT) + `POST /postmark-webhook` (Basic-auth bounce/complaint/unsubscribe suppression). | `backend/subscribe/src/{handler,crypto,store,email}.mjs`, `backend/subscribe/template.yaml` | 2026-07-02 |
| 11 | The Signal email distribution CLI | Operator `send-issue` CLI: fetch a published issue from Sanity, render HTML+text email, list confirmed recipients from DynamoDB, batch-send via Postmark Broadcast; dry-run by default, refuses to send without the unsubscribe placeholder. | `backend/broadcast/send-issue.mjs`, `backend/broadcast/src/*.mjs` | 2026-07-02 |
| 27 | Apply intake Lambda | `POST /apply` (SAM, scaffolded in #38, endpoint not yet wired to the form): length-capped field + email validation, silent honeypot drop, durable no-TTL DynamoDB application record, Postmark operator notification (ReplyTo = applicant); Postmark token from Secrets Manager at cold start. | `backend/apply/src/{handler,store,email}.mjs`, `backend/apply/template.yaml` | 2026-07-02 |

### E. Content management

| # | Feature | What it does | Where it lives | Last reviewed |
|---|---------|--------------|----------------|---------------|
| 12 | Sanity Studio for The Signal | Issue authoring CMS: `issue` document (slug/status/publishedAt/excerpt/body/seo), block content (incl. inline-code mark) + image objects, custom desk structure, Vision tool. | `studio/sanity.config.ts`, `studio/schemaTypes/**`, `studio/src/structure.ts` | 2026-07-02 |
| 13 | Build-time issue fetch | First build step: GROQ-query published issues from Sanity into a static JSON module the bundle and prerender both read; writes an empty list (build still succeeds) when unconfigured. | `scripts/fetch-issues.mjs`, `src/content/issues.generated.json` | 2026-07-02 |

### F. Cross-cutting frontend systems

| # | Feature | What it does | Where it lives | Last reviewed |
|---|---------|--------------|----------------|---------------|
| 14 | GSAP scroll choreography system | Declarative `<Fx>` wrapper translating `data-*` annotations (split/fade/stagger/draw/cells/count/tilt) into ScrollTrigger tweens, all gated inside `matchMedia(prefers-reduced-motion: no-preference)`. | `src/lib/fx.jsx` | 2026-07-02 |
| 15 | Pointer enhancements (cursor reticle + magnetic buttons) | Measurement-reticle cursor companion that frames interactive elements on hover and tracks scroll, plus a magnetic-pull effect on buttons (`initMagnetic()`); both fine-pointer-only and disabled under reduced motion. | `src/components/Cursor.jsx`, `src/lib/magnetic.js` | 2026-07-02 |
| 16 | Motion pause + reduced-motion system | WCAG 2.2.2 pause/resume control for auto-animating scenes, backed by a shared motion-paused store that also reuses the reduced-motion render path. | `src/components/MotionToggle.jsx`, `src/lib/motion.js` | 2026-07-02 |
| 17 | Navigation | Floating header with section scrollspy, scroll-progress hairline, over-dark→light treatment switch, anchor links, and mobile menu toggle. | `src/components/Nav.jsx`, `src/components/Mark.jsx` | 2026-07-02 |
| 18 | Footer | Site footer with operator byline (E-E-A-T) and supporting links/legal copy. | `src/components/Footer.jsx` | 2026-07-02 |
| 19 | Per-route SEO/meta head | SPA hook syncing title/description/canonical/robots and OG/Twitter tags per route (with homepage-reset semantics) so each route presents correctly to AT, search, and unfurls. | `src/lib/usePageMeta.js` | 2026-07-02 |
| 20 | Design system / tokens | Hand-written design system: 60/20/10/10 palette + semantic tokens, `.ground-dark` re-resolution, Archivo/IBM Plex Mono type scale, primitives & components, and the decorative pixel-mosaic motif (the DOSS signature, used in Proof/FinalCta/Footer). | `src/styles/tokens.css`, `src/styles/base.css`, `src/styles/components.css`, `src/components/Mosaic.jsx` | 2026-07-02 |

### G. SEO, build & ops

| # | Feature | What it does | Where it lives | Last reviewed |
|---|---------|--------------|----------------|---------------|
| 21 | Prerender + SSR pipeline | Renders indexable routes (`/`, `/story`, `/apply`, `/signal`, each issue) to static HTML after the SSR build, with per-route head injection, render-marker guards, and `data-route` hydration gating. | `scripts/prerender.mjs`, `scripts/lib/inject-head.mjs`, `src/entry-static.jsx` | 2026-07-02 |
| 22 | Structured data & social meta | JSON-LD `@graph` (Organization + founder Person + WebSite + OfferCatalog tiers) and full OG/Twitter card set in the static head. | `index.html` | 2026-07-02 |
| 23 | Sitemap & robots | Sitemap regenerated from prerendered routes (static + non-noindex issues) at build; static `robots.txt` with sitemap directive and AI-crawler rules. | `scripts/prerender.mjs` (sitemap block), `public/robots.txt` | 2026-07-02 |
| 24 | Security headers + CSP | Amplify `customHttp.yml`: HSTS, X-Frame-Options, nosniff, Referrer/Permissions policies, and an enforcing Content-Security-Policy scoped to self + the three API origins (api/checkout/apply, #39). | `customHttp.yml` | 2026-07-02 |
| 25 | CI / deploy test gate | preBuild + GitHub Actions gate: `npm test` (backend) + `npm run test:fe` (Vitest) + `npm run lint` must pass before build/publish; Node pinned to 22. | `amplify.yml`, `.github/workflows/ci.yml` | 2026-07-02 |
| 26 | Deploy verification script | Post-deploy curl check: deep links rewrite to the SPA shell, the shipped bundle references a live checkout API, sitemap serves statically. | `scripts/verify-deploy.sh` | 2026-07-02 |

## Optimization Opportunities

> Category A (features 1-3) audited 2026-06-27 via the `/optimize-features` lens
> fan-out: 11 read-only lens workers (5 visual-craft dimensions through the
> `ui-auditor` specialist + 6 functional lenses) each reviewed all three features
> and returned findings-contract records; merged, deduped, and spot-verified
> against the real code before persisting. Color/contrast came back clean (text
> contrast is excellent on both grounds — the only contrast item is a non-text
> control-border one). Impact ratings are post-verification.

### 1. Landing page scroll narrative
- [x] **[Typography]** Reduce the residual hero-wordmark swap-reflow — self-hosted Archivo (latin + latin-ext) with `font-display: optional` + a metrics-matched Arial fallback so the LCP wordmark never reflows; literal preload in index.html, fontsource Archivo dropped. Impact: Medium. Effort: Medium. (`src/styles/tokens.css`, `src/main.jsx`, `index.html`, `vite.config.js`) — added 2026-06-27; completed 2026-06-28 (#29) — verified live (preload + font-display:optional + /fonts served 200)
- [x] **[Performance]** Stop shipping SplitText + DrawSVGPlugin on routes that never animate — new `src/lib/gsap-core.js` holds the eager core (gsap + ScrollTrigger + useGSAP); the 6 non-choreography consumers (App, main, Cursor, magnetic, Nav, Hero) import from it, and `fx.jsx` dynamically `import()`s the two plugins inside its motion-gated path (gsap.context + `cancelled` guard for async cleanup). `vite.config.js` `manualChunks` excludes them from the eager gsap chunk. Impact: Medium. Effort: Medium. (`src/lib/gsap-core.js`, `src/lib/fx.jsx`, `src/components/{Cursor,Nav}.jsx`, `src/main.jsx`, `src/App.jsx`, `src/sections/Hero.jsx`, `src/lib/magnetic.js`, `vite.config.js`) — added 2026-06-27; completed 2026-06-28 — verified: eager gsap chunk 50.09→45.32 kB gz, SplitText/DrawSVG now runtime async chunks; `/signal` fetches no plugin chunks; browser smoke on `/` + `/story` confirms split/draw fire with a clean console.
- [x] **[Motion]** Fix compound reveal travel in the Inside stack groups — `data-fade` moved onto the group heading (sibling of the list) so it no longer compounds with the list's `data-stagger`. Impact: Medium. Effort: Low. (`src/sections/Inside.jsx`, `src/lib/fx.jsx`) — added 2026-06-27; completed 2026-06-28 (#30)
- [x] **[Architecture]** Data-drive the three pricing tiers — the three hand-written `<article className="tier">` blocks become a `TIERS` array + map (prerendered HTML verified structurally identical). Impact: Medium. Effort: Medium. (`src/sections/Inside.jsx`) — added 2026-06-27; completed 2026-06-28 (#30)
- [x] **[Accessibility]** Raise resting control-border contrast to 3:1 — new `--control-edge` token (~3.2:1 on ghost, ~5.1:1 on black) on ghost buttons + form inputs (WCAG 1.4.11). Impact: Medium. Effort: Low. (`src/styles/tokens.css`, `src/styles/base.css`, `src/styles/components.css`) — added 2026-06-27; completed 2026-06-28 (#28) — affects features 1 & 3 + Apply
- [x] **[Resilience & tests]** Add render smoke tests for the landing sections — per-section smoke tests for Hero (HeroScene mocked), Problem, Proof, Inside, FinalCta (BlochFigure mocked). Impact: Medium. Effort: Low. (`src/sections/*.test.jsx`) — added 2026-06-27; completed 2026-06-28 (#31)
- [x] **[Cleaner]** Reuse `MatrixCell` for the `is-us` column. Impact: Low. Effort: Low. (`src/sections/Proof.jsx`) — added 2026-06-27; completed 2026-06-28 (#32)
- [x] **[Cleaner]** De-duplicate the two stack-groups and remove the magic numbering offset — `<StackGroup>` with derived `startNum` + `padStart` (no more `0{i+5}` / "010"). Impact: Low. Effort: Low. (`src/sections/Inside.jsx`) — added 2026-06-27; completed 2026-06-28 (#30)
- [x] **[Cleaner]** Drop the derivable `STATS.value` — render the static fallback from the count parts. Impact: Low. Effort: Low. (`src/sections/Proof.jsx`) — added 2026-06-27; completed 2026-06-28 (#32)
- [x] **[Consistency]** Move the inline wave-particle divider into its own component — extracted `<WaveDivider/>`. Impact: Low. Effort: Low. (`src/sections/WaveDivider.jsx`, `src/pages/Landing.jsx`) — added 2026-06-27; completed 2026-06-28 (#32)
- [x] **[Accessibility]** Mirror the FAQ summary hover color on `:focus-visible`. Impact: Low. Effort: Low. (`src/styles/components.css`) — added 2026-06-27; completed 2026-06-28 (#32)
- [x] **[Typography]** Add a `--text-display-sm` token for the mid-display heading size and point the three ad-hoc clamps at it. Impact: Low. Effort: Low. (`src/styles/tokens.css`, `src/styles/components.css`) — added 2026-06-27; completed 2026-06-28 (#32)

### 2. The Story page
- [x] **[Accessibility]** Fix the heading-level skip on the Story page — promoted the two `.story-panel` headings `h3` → `h2` (WCAG 1.3.1), display style preserved. Impact: Medium. Effort: Low. (`src/sections/Story.jsx`, `src/styles/components.css`) — added 2026-06-27; completed 2026-06-28 (#28) — verified live (h2 panels on /story)
- [x] **[UX]** Relabel the Story page's "Back to the page" CTA → "Go to the home page". Impact: Low. Effort: Low. (`src/pages/Story.jsx`) — added 2026-06-27; completed 2026-06-28 (#28) — verified live
- [x] **[Resilience & tests]** Add a render test for the Story page (renders + CTA hrefs + h2 heading-order lock). Impact: Low. Effort: Low. (`src/pages/Story.test.jsx`) — added 2026-06-27; completed 2026-06-28 (#31)
- (Hero-wordmark FOUT, item under feature 1, also applies to /story — shipped in #29.)

### 3. The Signal web archive
- [x] **[Layout]** Un-cramp the subscribe widget on the issue reader — `.signal-issue .signal { grid-template-columns: 1fr }` forces the widget single-column in the 52rem reading column. Impact: High. Effort: Low. (`src/styles/components.css`) — added 2026-06-27; completed 2026-06-28 (#26) — latent until the first issue ships (no issue pages to soak yet)
- [x] **[Performance]** Size Signal issue images through the Sanity CDN and reserve their space — sized `?w=…&auto=format` src + 2x srcset + forwarded width/height + `aspect-ratio` fallback (CLS). Impact: Medium. Effort: Low. (`src/components/IssueBody.jsx`, `src/styles/components.css`) — added 2026-06-27; completed 2026-06-28 (#27)
- [x] **[Security]** Harden the Portable Text link serializer — allowlist http(s)/mailto/tel/in-app; anything else (`javascript:`/`data:`, `//host`, empty) renders as plain text. Impact: Medium. Effort: Low. (`src/components/IssueBody.jsx`) — added 2026-06-27; completed 2026-06-28 (#27)
- [x] **[Resilience]** Stop `formatDate` printing "Invalid Date" — returns `''` on a `NaN` date; `<time>` rendered only when non-empty. Impact: Medium. Effort: Low. (`src/lib/formatDate.js`, `src/pages/Signal.jsx`, `src/pages/SignalIssue.jsx`) — added 2026-06-27; completed 2026-06-28 (#27)
- [x] **[UX]** Tell the reader when an issue link is dead — redirect carries `state:{notFound:slug}`; the index shows a "that issue isn't available" notice. Impact: Medium. Effort: Low. (`src/pages/SignalIssue.jsx`, `src/pages/Signal.jsx`) — added 2026-06-27; completed 2026-06-28 (#26)
- [x] **[Resilience & tests]** Cover the Signal unhappy paths — empty-archive (live render), sparse issue (null date/empty body), IssueBody missing-image/missing-href/list cases. Impact: Medium. Effort: Low. (`src/pages/Signal.empty.test.jsx`, `src/pages/SignalIssue.test.jsx`, `src/components/IssueBody.test.jsx`) — added 2026-06-27; completed 2026-06-28 (#27 + #31)
- [x] **[Motion]** Gate the archive-row hover and add keyboard parity — hover `translateX` gated under `prefers-reduced-motion`, `:focus-visible` parallel added, `overflow-x: clip` on `.signal-archive`. Impact: Medium. Effort: Low. (`src/styles/components.css`) — added 2026-06-27; completed 2026-06-28 (#26)
- [ ] **[Consistency]** Bring the Signal pages onto the house visual scaffolding — both render bare `<div className="signal-archive">`/`<article className="signal-issue">` with bespoke head classes, skipping the shared `kicker` + `section-title` system every other page (incl. the analogous Story page) uses. Adopt the shared header scaffolding (heavy scroll motion stays optional for a reading surface). Impact: Medium. Effort: Medium. (`src/pages/Signal.jsx:15-19`, `src/pages/SignalIssue.jsx:22-29`, `src/sections/Story.jsx:5-10`) — added 2026-06-27 — OPEN: not in the Category-A execution batch; a candidate for a future pass.
- [x] **[Typography]** Add an inline-`code` mark renderer to `IssueBody` — renders `<code>` styled on the IBM Plex Mono system via `.issue-body code`. Impact: Low. Effort: Low. (`src/components/IssueBody.jsx`, `src/styles/components.css`) — added 2026-06-27; completed 2026-06-28 (#32)
- [ ] **[Performance]** Keep full issue bodies out of the main bundle — `issues.js` statically imports `issues.generated.json` (every issue's full body) and `App.jsx` imports Signal/SignalIssue eagerly, so the whole corpus rides the main chunk on every route; only `/signal/:slug` needs bodies, and those pages are already prerendered. Move bodies behind a per-slug dynamic import. Impact: Low. Effort: Medium. (`src/lib/issues.js:1`, `src/App.jsx:9-10`) — added 2026-06-27 — DEFERRED: no cost today (archive empty); revisit if the corpus grows.

> Category B (features 4-6) audited 2026-06-28 via the `/optimize-features`
> verified workflow: 24 feature×lens reviews → 59 raw findings → each
> adversarially refuted (evidence-holds / refinement-not-new-feature /
> intentional-pattern / honest-impact gates) → 55 confirmed, 4 dropped → deduped
> to the distinct items below (the fan-out surfaced the cross-cutting items —
> missing tests for the three untested pages, the duplicated intake state machine,
> param-left-in-URL — many times each). Impact ratings are post-verification: the
> verifier demoted most Medium→Low, leaving 4 genuine Medium items. Honest current
> state: the live-payment guards (open-redirect, https transport, session
> re-verification, bfcache reset) are already correct and well-covered where they
> exist; the gaps are validation feedback, the three untested page state machines,
> and transient-failure UX on the two verifier pages.

### 4. Membership application (`/apply`)
- [x] **[UX/Accessibility]** Validation failure is communicated by border color only — `handleSubmit` sets `attempted` and shows one generic sentence; the 8 invalid fields get a red border (`.was-validated …:invalid`) but no `aria-invalid`, no per-field message, no programmatic error association. Add `aria-invalid` to invalid controls and a per-field error line tied via `aria-describedby` (WCAG 3.3.1/4.1.3). Impact: Medium. Effort: Medium. (`src/pages/Apply.jsx:67-89,152-292`, `src/styles/components.css:1505-1507`) — added 2026-06-28; completed 2026-06-28 (#35) — errors also clear live as each field is fixed
- [x] **[Resilience & tests]** `Apply.jsx` — the most complex intake form (8 fields, client validation, preview/sent/error states, focus parking) — has zero test coverage while the simpler `SignalSubscribe` ships two test files. Add `Apply.test.jsx`: invalid-submit focuses first invalid + shows the alert, valid-submit POSTs `{form:'apply',…}` and shows success with focus parked, unset-endpoint shows the honest preview. Impact: Medium. Effort: Medium. (`src/pages/Apply.jsx` — no `Apply.test.jsx`) — added 2026-06-28; completed 2026-06-28 (#35 — `Apply.test.jsx` + `Apply.preview.test.jsx`)
- [x] **[UI]** `.btn` has no `:disabled`/busy visual state — during `sending` the button is `disabled`+`aria-busy` but `.btn` hard-codes `cursor:pointer` and there is no `:disabled` rule anywhere in `src/styles`, so a busy button still reads as fully pressable. Add `.btn:disabled` (reduced opacity, `cursor:not-allowed`). Cross-cutting: fixes every primary CTA. Impact: Low. Effort: Low. (`src/styles/base.css:165-191`, `src/pages/Apply.jsx:301-313`) — added 2026-06-28; completed 2026-06-28 (#35)
- [x] **[Architecture/Cleaner]** `Apply` and `SignalSubscribe` independently reimplement the same `idle→sending→sent/preview/error` machine (same 5-state comment, same `!ENDPOINT`→preview short-circuit, same `await postJson`→sent/catch→error, same `aria-busy`/disabled/label-swap button). Extract a `useIntakeSubmit(endpoint, buildPayload)` hook in `src/lib`; keep each surface's own JSX. Scope to the `postJson` flow only (Activate/Welcome/Confirm differ). Impact: Low. Effort: Medium. (`src/pages/Apply.jsx:50-89`, `src/components/SignalSubscribe.jsx:12-37`) — added 2026-06-28; completed 2026-06-28 (#35 — `src/lib/useIntakeSubmit.js`, with unmount guard)
- [x] **[Cleaner]** The `applicantType`/`stage`/`modality` dropdowns are three near-verbatim `.field`+label+`<select>`+disabled-placeholder+`.map()` blocks differing only by id/name/label/source array. Collapse into one local `SelectField` component (~30 lines, one place to change select markup). Impact: Low. Effort: Low. (`src/pages/Apply.jsx:220-272`) — added 2026-06-28; completed 2026-06-28 (#35)
- [x] **[UX/Resilience]** On submit error focus stays on the submit button rather than moving to the recovery `role="alert"`; and the in-flight POST has no unmount guard (a `cancelled` flag like `Confirm.jsx` uses), so `setStatus` can fire on an unmounted form. Move focus to the error and add the guard. Impact: Low. Effort: Low. (`src/pages/Apply.jsx:67-89,294-299`) — added 2026-06-28; completed 2026-06-28 (#35 — guard lives in the shared hook)
- [x] **[Security]** The Apply form omits the honeypot `website` field that the sibling Signal form ships (and the subscribe backend already checks). When `VITE_APPLY_ENDPOINT` goes live the apply intake will accept naive bot posts the Signal path silently drops. Add the same off-screen `aria-hidden` honeypot. Impact: Low. Effort: Low. (`src/pages/Apply.jsx` vs `src/components/SignalSubscribe.jsx:68-78`) — added 2026-06-28; completed 2026-06-28 (#35 — sent in the payload for the live endpoint to drop)
- [x] **[UX]** The `role` field has no `autocomplete`, so browsers can't autofill job title on a high-intent gated form (every other field has the correct token). Add `autocomplete="organization-title"`. Impact: Low. Effort: Low. (`src/pages/Apply.jsx:207-216`) — added 2026-06-28; completed 2026-06-28 (#35)
- [x] **[Accessibility]** All 8 fields are `required` with no advance indication to sighted users (failure is only discoverable on submit); the success/preview views drop the document to an `h2` with no `h1`; and `id="apply-title"` is never wired to the form via `aria-labelledby`. Add a required affordance, promote the standalone success heading, and wire the label. Impact: Low. Effort: Low. (`src/pages/Apply.jsx:104-107,137,152-157,160-285`) — added 2026-06-28; completed 2026-06-28 (#35)
- [x] **[UI/Consistency]** Error color (`.form-error`) is a magic literal, not a first-class token like every other color on the form; and the aside skips the physics-notation kicker (`<p class="kicker">…`) its near-identical sibling `Activate` leads with. Tokenize the error color and add the kicker for cross-page consistency. Impact: Low. Effort: Low. (`src/styles/components.css:1511`, `src/pages/Apply.jsx:136-137` vs `src/pages/Activate.jsx:69-71`) — added 2026-06-28; completed 2026-06-28 (#35 — `--error-ink` token; aside kicker `|ψ⟩`)

### 5. The Signal signup + double opt-in
- [x] **[Resilience & tests]** The `/confirm` double-opt-in verify path — the only client component that handles a credential — has zero test coverage. Add `Confirm.test.jsx`: token→`postJson(/verify,{token})`→confirmed, rejected token→invalid, missing token/endpoint→unknown, and unmount cancels the in-flight verify. Impact: Medium. Effort: Medium. (`src/pages/Confirm.jsx` — no `Confirm.test.jsx`) — added 2026-06-28; completed 2026-06-28 (#35 — also covers transient-retry + URL-strip)
- [x] **[UX/Resilience]** `Confirm` collapses every `postJson` rejection into "This link has expired" — a network blip, 5xx, or timeout shows a permanent dead-end with no retry, indistinguishable from a genuinely consumed token. Distinguish transient failure (offer "try again") from a 4xx invalid-token (the expired copy). Impact: Medium. Effort: Medium. (`src/pages/Confirm.jsx:31-41,71-82`, `src/lib/submit.js:20`) — added 2026-06-28; completed 2026-06-28 (#35 — branches on `err.status`; also strips the consumed token from the URL)
- [x] **[Cleaner/Security]** `VERIFY_ENDPOINT` is derived by `SIGNAL_ENDPOINT.replace(/\/subscribe$/, '/verify')` — if the subscribe URL ever doesn't end in `/subscribe` the regex silently no-ops and the token POSTs back to the subscribe route. Derive both routes from a single base + explicit suffix (or assert the rewrite changed the string). Impact: Low. Effort: Low. (`src/pages/Confirm.jsx:9`) — added 2026-06-28; completed 2026-06-28 (#35 — `verifyEndpoint()` swaps the trailing segment)
- [x] **[Accessibility]** On success `SignalSubscribe` replaces the form with the success copy but never moves focus, dropping it to `<body>` (unlike `Apply`, which parks focus on the success heading). Park focus on the success message. Impact: Low. Effort: Low. (`src/components/SignalSubscribe.jsx:39-61`) — added 2026-06-28; completed 2026-06-28 (#35)
- [x] **[Accessibility]** The error `<p role="alert">` is mounted fresh on error rather than living in a persistent region, against the codebase's own repeated "live region must outlive the state swap" rule. Move it inside a persistent wrapper. Impact: Low. Effort: Low. (`src/components/SignalSubscribe.jsx:102-106`) — added 2026-06-28; completed 2026-06-28 (#35 — persistent `role="alert"` wrapper)
- [x] **[UI]** The success/preview confirmation box (`.signal-success`) is low-contrast when the widget renders on the light-ground archive/issue pages; and the error message butts directly against the submit button with zero spacing. Add a contrast-safe treatment for the light surface and margin above the error. Impact: Low. Effort: Low. (`src/components/SignalSubscribe.jsx:50-60,102-106`, `src/styles/components.css:1396-1401`) — added 2026-06-28; completed 2026-06-28 (#35 — border uses `--accent`; `.signal-alert` spacing)
- [x] **[Consistency]** The Signal error copy omits the "nothing was lost" reassurance every other intake error carries, and the primary submit button drops the `btn-arrow` glyph used on every other primary CTA. Align both. Impact: Low. Effort: Low. (`src/components/SignalSubscribe.jsx:92-99,102-106`) — added 2026-06-28; completed 2026-06-28 (#35)
- [x] **[Cleaner]** The `{...(id ? { id } : {})}` conditional id-prop spread is a dead idiom — a plain `id={id}` is equivalent (React omits `undefined`). Simplify. Impact: Low. Effort: Low. (`src/components/SignalSubscribe.jsx:40`) — added 2026-06-28; completed 2026-06-28 (#35)

### 6. Membership activation & Stripe Checkout flow
- [x] **[Resilience & tests]** `Welcome.jsx` — the entire post-payment verification state machine (`checking/active/processing/incomplete/unknown`, the `status`+`payment_status` gating that makes "membership active" a receipt not a vibe) — has zero test coverage, while its partner `Activate` has two test files. Add `Welcome.test.jsx` covering each branch + the session-field rendering. Impact: Medium. Effort: Medium. (`src/pages/Welcome.jsx` — no `Welcome.test.jsx`) — added 2026-06-28; completed 2026-06-28 (#35)
- [x] **[UX/Resilience]** A paid member whose `GET /session` call fails (network/5xx/timeout) falls into `unknown` — the same reassuring "Welcome" a cold no-`session_id` visitor sees, with no retry. Split `unknown` (no session id) from a new `error` state (verification failed → "couldn't confirm yet, retry") so a real member isn't told everything's fine when it isn't. Impact: Medium. Effort: Medium. (`src/pages/Welcome.jsx:18-43,105-116`) — added 2026-06-28; completed 2026-06-28 (#35 — new `error` state with retry; payment-safe copy)
- [x] **[Security]** The `session_id` (an unauthenticated bearer for customer PII via `/session`) and the Confirm `token` are left in the URL and browser history after they're consumed. `history.replaceState` to strip the query param once verification resolves. Cross-cutting with feature 5. Impact: Low. Effort: Low. (`src/pages/Welcome.jsx:22-43`, `src/pages/Confirm.jsx:24-41`) — added 2026-06-28; completed 2026-06-28 (#35 — both pages strip on resolve)
- [x] **[Accessibility]** `Welcome` (and `Confirm`) render an orphan `h2` with no `h1` in every state; the decorative physics-notation kicker glyphs (`|0⟩`, `Δ`) are exposed to screen readers and read aloud as garbled symbols. Promote one heading per page to `h1` and `aria-hidden` the decorative glyphs (or give them an sr-only label). Cross-cutting Confirm/Activate. Impact: Low. Effort: Low. (`src/pages/Welcome.jsx:52-116`, `src/pages/Confirm.jsx:50-54`, `src/pages/Activate.jsx:69-71`) — added 2026-06-28; completed 2026-06-28 (#35 — h1 + glyphs `aria-hidden` across all three pages)
- [x] **[UX/UI]** The Activate checkout error appears above the submit button with no focus or scroll management (a member who scrolled past won't see it), and the activation-success card has no visual hierarchy over the error/neutral states. Move focus to the error on failure; give the success card distinct emphasis. Impact: Low. Effort: Low. (`src/pages/Activate.jsx:47-58,113-118`, `src/styles/components.css:1612-1627`) — added 2026-06-28; completed 2026-06-28 (#35 — error focus; `.welcome-card` accent edge)
- [x] **[Consistency]** `Welcome` drops the `|0⟩` kicker on its `processing` state (every sibling state and `Confirm` carry it), and `Activate`'s preview state carries a vestigial `aria-live` on a node that never swaps (diverging from the persistent-wrapper convention). Restore the kicker; drop the dead `aria-live`. Impact: Low. Effort: Low. (`src/pages/Welcome.jsx:82-93`, `src/pages/Activate.jsx:141`) — added 2026-06-28; completed 2026-06-28 (#35)
- [x] **[Resilience & tests]** Activate's documented bfcache `pageshow` reset (the guard that stops "Opening secure checkout…" sticking forever on Back from Stripe) is untested. Add a test that fires a `persisted` `pageshow` and asserts the button re-enables. Impact: Low. Effort: Low. (`src/pages/Activate.jsx:39-45`) — added 2026-06-28; completed 2026-06-28 (#35)
- [ ] **[Architecture]** `Welcome` and `Confirm` are the same shape — read a URL param, POST/GET it against the backend, drive a confirming→resolved state machine with the same persistent-live-region + cancelled-flag scaffolding. An optional `useVerifiedParam(param, verifyFn)` hook would remove the duplication; weigh against the two pages' genuinely different field handling. Impact: Low. Effort: Medium. (`src/pages/Welcome.jsx:22-43`, `src/pages/Confirm.jsx:24-41`) — added 2026-06-28 — DEFERRED (#35): the two pages diverge enough (GET vs POST, different fields/states) that the shared hook wasn't judged worth the indirection; revisit if a third verifier page appears
- [x] **[Cleaner]** `Welcome.jsx` doesn't indent its state sections under the `aria-live` wrapper `div`, hiding the live-region boundary on read (its sibling `Confirm`/`Apply` indent theirs). Re-indent for legibility. Impact: Low. Effort: Low. (`src/pages/Welcome.jsx:50-117`) — added 2026-06-28; completed 2026-06-28 (#35)

> Category C (features 7-8) audited 2026-07-02 via the `/optimize-features` lens
> fan-out: 8 read-only lens workers (Performance via the R3F specialist agent,
> Accessibility, Resilience & tests, UX, Cleaner/Architecture, Consistency, plus
> ui-auditor Motion and Component-craft dimensions) → 25 raw findings → merged
> and spot-verified against the real code by the main thread → 21 distinct items.
> Security and Observability lenses skipped honestly (no user input, no runtime
> backend). Three independent lenses converged on the fig. 03 pause gap and three
> on the hardcoded WebGL palette — treated as strong confirmation. Honest current
> state: both features are carefully built (deterministic seed, DPR clamp,
> frameloop gating, error boundaries, SVG fallbacks all real and correct); the
> gaps are a pause-control blind spot, zero test coverage of the wrapper/fallback
> logic, and drift surfaces (palette, duplicated scaffold, per-figure stroke
> language).

### 7. Hero ground-state 3D scene
- [x] **[Resilience & tests]** The HeroScene wrapper's resilience logic is 100% mocked away — `Hero.test.jsx:7` stubs the whole component, so the SceneBoundary null-fallback, the `prefersReducedMotion` probe, the `useMotionPaused` integration, and the IntersectionObserver `active` gating never run in any test. Add `HeroScene.test.jsx` mocking only the leaf `GroundStateScene` (stub echoing props): assert `reduced` under matchMedia-reduce and under `setMotionPaused(true)`, and that a throwing scene renders null without crashing the hero. Impact: Medium. Effort: Low. (`src/sections/Hero.test.jsx:7`, `src/components/HeroScene.jsx:9-17,29-32,50`) — added 2026-07-02; completed 2026-07-02 (#40)
- [x] **[UX/Performance]** The 2400-particle loop ramps up mid-entrance — `frameloop` is `'always'` as soon as `active` (`src/three/GroundStateScene.jsx:249`) and `inView` initializes `true` (`src/components/HeroScene.jsx:28`), so the sim's heaviest main-thread work can land on top of the ~1.1s wordmark entrance (`src/sections/Hero.jsx:27`) and the `fonts.ready` ScrollTrigger refresh (`src/main.jsx:23`). Gate `active` behind one idle frame (requestIdleCallback/short timeout) in `HeroScene.jsx` so the entrance completes before the sim ramps; no visual change. Impact: Medium. Effort: Low. — added 2026-07-02; completed 2026-07-02 (#40)
- [x] **[Architecture/Cleaner]** `HeroScene` and `BlochFigure` duplicate the entire scene-lifecycle scaffold — identical error-boundary classes (`SceneBoundary` returns null, `FigBoundary` returns `props.fallback`) and two copies of `prefersReducedMotion()` (one with the SSR guard, one without). Extract one shared boundary (fallback prop defaulting null) and move `prefersReducedMotion` into `src/lib/motion.js` next to `useMotionPaused`; matches the `gsap-core.js` shared-core precedent. Spans features 7+8. Impact: Medium. Effort: Low. (`src/components/HeroScene.jsx:9-24`, `src/components/figures/BlochFigure.jsx:10-22`) — added 2026-07-02; completed 2026-07-02 (#40)
- [x] **[Consistency]** The WebGL palette is hand-copied in three places — `GroundStateScene.jsx:20-23` and `BlochScene.jsx:13-15` each redeclare the exact `--ghost/--powder/--sand/--black` hexes from `tokens.css:53-57`, so a palette edit silently never reaches the scenes (faithful by convention, not construction; flagged independently by 3 lenses). Add `src/lib/palette.js` exporting GHOST/POWDER/SAND/BLACK, commented as mirroring tokens.css, imported by both scenes. Impact: Medium. Effort: Low. — added 2026-07-02; completed 2026-07-02 (#40)
- [x] **[Resilience]** WebGL context LOSS after creation is unhandled in both scenes — the boundaries only catch throws; `webglcontextlost` is a DOM event, so a GPU reset freezes the canvas on its last frame instead of reverting to the existing fallbacks. Add an `onCreated={({gl}) => …addEventListener('webglcontextlost', …)}` that lifts a flag forcing the fallback (null for hero, `<BlochSphere/>` for fig. 04). Bounded: both canvases are decorative/`aria-hidden`. Impact: Low. Effort: Medium. (`src/three/GroundStateScene.jsx:246-260`, `src/three/BlochScene.jsx:136-146`) — added 2026-07-02; completed 2026-07-02 (#40)
- [x] **[UX]** On WebGL failure the hero is a bare black cell with an orphaned caption and a no-op Pause control — `SceneBoundary` renders null (`HeroScene.jsx:16`), while `scene-ket`/`scene-caption` and `MotionToggle` render unconditionally (`src/sections/Hero.jsx:87,94`), describing/controlling a figure that isn't there. Lift the failed flag and let `Hero.jsx` gate the caption + toggle on "scene actually running". Impact: Low. Effort: Medium. — added 2026-07-02; completed 2026-07-02 (#40)
- [x] **[Cleaner]** Shared three-scene primitives are copy-defined — the per-frame delta clamp `Math.min(rawDt, 0.05)` is duplicated verbatim (`GroundStateScene.jsx:121`, `BlochScene.jsx:73`) and `BlochScene`'s `circleGeometry()` helper (`BlochScene.jsx:23-29`) is reinvented inline three times by `Well()` (`GroundStateScene.jsx:183-187,203-207`). Fold into a small `src/three/shared.js` (or into the palette module's sibling). Impact: Low. Effort: Medium. — added 2026-07-02; completed 2026-07-02 (#40)
- [x] **[Consistency]** Reduced-motion is handled internally by `GroundStateScene` (a `reduced` prop renders a static frame) but delegated away by `BlochScene` (its wrapper swaps in the SVG; the scene has no reduced concept). The divergence is defensible (fig. 04 has an accurate SVG twin; the hero doesn't) — document it with a one-line comment in `BlochScene.jsx` pointing at `BlochFigure` as the reduced-motion owner. Impact: Low. Effort: Low. (`src/three/GroundStateScene.jsx:246`, `src/three/BlochScene.jsx:136`, `src/components/figures/BlochFigure.jsx:54`) — added 2026-07-02; completed 2026-07-02 (#40)
- [x] **[Consistency]** `HeroScene.jsx` sits in `components/` while its structurally identical twin `BlochFigure.jsx` lives in `components/figures/` with the rest of the figure family. Move `HeroScene.jsx` into `components/figures/` and update the import in `Hero.jsx` so the figures/ boundary is uniform. Impact: Low. Effort: Low. — added 2026-07-02; completed 2026-07-02 (#40)
- [x] **[Accessibility]** The MotionToggle's 1px border uses `--line-strong` (~2.9:1 on the black grounds both toggles sit on), just under the 3:1 of WCAG 1.4.11, while `--control-edge` (~5.1:1, purpose-built for control boundaries in #28) already exists. Switch `.motion-toggle` to `var(--control-edge)`. Impact: Low. Effort: Low. (`src/styles/components.css:306`, `src/styles/tokens.css:135-136`) — added 2026-07-02; completed 2026-07-02 (#40)
- [x] **[Performance]** The potential-well line art renders as ~12 draw calls / 12 material instances — 7 rings, 4 rails, and the E₀ ring each emit their own `<line>` + inline `<lineBasicMaterial>` though they share two opacity groups. Merge same-opacity groups into shared geometry/materials (~12 → ~3 draws), pixel-identical. Honest note: the dominant frame cost is the 2400-particle loop, which is intrinsic to the documented physics — this is the only clean behavior-preserving win, and it is small. Impact: Low. Effort: Medium. (`src/three/GroundStateScene.jsx:217,222,133`) — added 2026-07-02; completed 2026-07-02 (#40)

### 8. Physics SVG figures
- [x] **[Accessibility/Motion]** fig. 03's hν excitation arrow escapes the Pause-motion control — `.el-excite` pulses forever (`el-breathe 4.5s … infinite`, `components.css:1864-1867`) gated only on `prefers-reduced-motion`, while the pause store is React-only state scoped by its own comment to "the two perpetually animating scenes" (`src/lib/motion.js:4-6`) and sets no DOM hook. A visitor who clicks Pause motion stops both 3D scenes but this third perpetual animation keeps breathing (converged: a11y + motion + craft lenses). Fix at the mechanism level: have `setMotionPaused` (and the initial localStorage read) mirror state to `document.documentElement` (`data-motion-paused`), then gate the animation via `:root[data-motion-paused] .el-excite { animation: none }`. Impact: Medium. Effort: Low. (`src/components/figures/EnergyLevels.jsx:48`) — added 2026-07-02; completed 2026-07-02 (#41)
- [x] **[Resilience & tests]** `BlochFigure`'s WebGL-probe→SVG fallback — the default path for every no-WebGL/reduced-motion visitor — has zero tests despite being fully jsdom-testable with no mocking (`hasWebGL()` returns false in jsdom, so `<BlochFigure/>` renders `<BlochSphere/>` directly); `FinalCta.test.jsx:7` stubs it away entirely. Add `BlochFigure.test.jsx`: assert the SVG `role="img"` fallback renders, and still renders after `setMotionPaused(true)`. Impact: Medium. Effort: Low. (`src/components/figures/BlochFigure.jsx:24-31,54,83`) — added 2026-07-02; completed 2026-07-02 (#41)
- [x] **[UI craft]** The four figures don't share one drawing hand — five ad-hoc stroke widths (1 / 1.3 / 1.4 / 1.6 / 2.0), three dash rhythms ('2 6', '3 5', '2 4'), and three different guide-line tokens (`--line`, `--fig-stroke`, `--line-strong`, plus `--ink-soft` in EnergyLevels), across SVGs drawn at very different viewBox scales with no `vector-effect` — so the same nominal stroke renders ~2.4px in the wave but ~1.5px in the energy diagram, and on a 390px viewport the wave's 1.4 collapses to ~0.68px (sub-pixel, faint). Add `vector-effect="non-scaling-stroke"` to the drawn strokes and collapse to a 3-step weight scale + one dash cadence. Impact: Medium. Effort: Low. (`src/components/figures/WaveParticle.jsx:45-46`, `BlochSphere.jsx:21-24,44`, `EnergyLevels.jsx:29`, `src/styles/components.css:654-657`) — added 2026-07-02; completed 2026-07-02 (#41)
- [x] **[UX/Motion]** The WaveDivider's heavy scrub makes fast scrollers see a half-drawn wave that finishes off-screen — `data-draw-scrub="2.5"` with `data-draw-end="bottom 22%"` means a flick leaves the 2.5s catch-up completing after the figure has exited the viewport, the opposite of the code's own "watched, never glimpsed" intent comment. Lower the scrub toward the fx default (~1) and pull the end earlier (e.g. `bottom 70%`) so the wave completes while still comfortably in view. Impact: Medium. Effort: Low. (`src/sections/WaveDivider.jsx:12,16`, `src/lib/fx.jsx:147-150`) — added 2026-07-02; completed 2026-07-02 (#41)
- [x] **[Resilience & tests]** The pure SVG figures (`WaveParticle`, `EnergyLevels`, `BlochSphere`, `FigCaption`) are deterministic render functions with no tests — a broken aria-label, dropped energy level, or wrong figure number ships silently, and these SVGs are also the fallbacks for the 3D figures. Add a small `figures.test.jsx` asserting each figure's accessible name/content and FigCaption's `fig. {num}` + children. Impact: Low. Effort: Low. (`src/components/figures/WaveParticle.jsx:36-57`, `EnergyLevels.jsx:6-10,21-41`, `FigCaption.jsx:2-14`) — added 2026-07-02; completed 2026-07-02 (#41)
- [x] **[UX]** During a slow chunk load the Bloch figure is a blank cell despite the accurate SVG being right there — the lazy `BlochScene`'s Suspense fallback is `null` (`BlochFigure.jsx:113`) while `<BlochSphere/>` is already this component's fallback for every other path. Use `<BlochSphere/>` as the Suspense fallback too. Impact: Low. Effort: Low. — added 2026-07-02; completed 2026-07-02 (#41)
- [x] **[Cleaner/Consistency]** One decision resolves two convergent findings: `FigCaption`'s `ket` prop + branch + `.fig-ket` CSS are dead (no caller passes `ket` — clean lens), while fig. 01 in the Hero hand-rolls exactly that markup with ad-hoc `scene-caption`/`scene-ket` elements (consistency lens). Either route fig. 01's caption through `FigCaption` (making `ket` live) where the overlay layout allows, or delete the dead `ket` path and comment the hero's deliberate divergence. Impact: Low. Effort: Medium. (`src/components/figures/FigCaption.jsx:7-11`, `src/sections/Hero.jsx:87`, `src/styles/components.css:644`) — added 2026-07-02; completed 2026-07-02 (#41) — resolved by deleting the dead `ket` path: the hero caption is an absolutely-positioned overlay on the live canvas, not a flow caption, so the divergence is deliberate and now documented in FigCaption.jsx
- [x] **[Cleaner]** The arrowhead `<marker>` is duplicated byte-for-byte between `EnergyLevels` (`el-arrow`) and `BlochSphere` (`bs-arrow`) — same viewBox, path, and `var(--accent-display)` fill. Extract a tiny `figures/ArrowMarker.jsx` (id prop; each SVG still needs its own def instance). Impact: Low. Effort: Low. (`src/components/figures/EnergyLevels.jsx:44-47`, `BlochSphere.jsx:16-19`) — added 2026-07-02; completed 2026-07-02 (#41)
- [x] **[Accessibility]** The live Bloch container's `role="img"` accessible name ends "Drag to rotate the view." — a pointer-only affordance (rotation is `onPointerMove` only) announced to users who can't use it, while the visible FigCaption already carries the drag hint for sighted pointer users. Drop the clause from the aria-label (keyboard rotation would be new functionality — out of refine scope). Impact: Low. Effort: Low. (`src/components/figures/BlochFigure.jsx:90,100-101`, `src/sections/FinalCta.jsx:77`) — added 2026-07-02; completed 2026-07-02 (#41)
- [x] **[Performance]** DrawSVG setup resolves every shape's computed style twice — `fx.jsx:138-139` buckets stroked vs fill-only shapes with two `.filter()` passes each calling `getComputedStyle()` (36 forced style reads for WaveParticle's 18 shapes where 18 suffice; one-time at reveal, not per-frame). Replace with a single pass pushing into both buckets. Touches `fx.jsx` but scoped to the figures' draw path. Impact: Low. Effort: Low. (`src/lib/fx.jsx:138-139`, `src/components/figures/WaveParticle.jsx:45`) — added 2026-07-02; completed 2026-07-02 (#41)

> Category G (features 21-26) audited 2026-06-27 via the `/optimize-features`
> verified workflow: 29 feature×lens reviews → 117 raw findings → adversarially
> verified against the real code → deduped to the items below. Impact ratings are
> the post-verification ratings, with a few elevated to High where the issue is a
> live, user-visible break rather than a latent one.

### Execution status — 2026-06-27 (shipped to `main`, deployed, verified live)

Category G is **closed**. All work merged to `main` and deployed via Amplify (build job 30
SUCCEED — the preBuild gate ran `npm test` + `test:fe` + lint + `check-csp` + `npm run build`
green in the real environment). PRs:
- **#18** — prerender/SEO/build hardening + test gate (21, 22, 23, 25).
- **#21** — CSP: Sanity images, `form-action`, `check-csp.mjs` connect-src guard (24).
- **#20** — deploy verification rewrite + CI hardening (26, 24-headers).
- **#22** — CI build-on-PR + SHA-pinned actions (25 low-tail).
- **#23** — checkout custom-domain IaC (ACM us-east-2 + API Gateway v2 domain + Route 53).
- **#24** — CSP `connect-src` pinned to `checkout.groundstatesociety.com`; region wildcard removed (24).
- **#25** — low-tail: samconfig profile pins, `SITE` de-dup (`src/lib/site.js`), `og:image:alt`,
  homepage-head drift-guard test, robots sync note.

**Verified live (not just green tests):** `verify-deploy.sh` all green; CSP `connect-src` is now
`'self' https://api.groundstatesociety.com https://checkout.groundstatesociety.com` (wildcard
confirmed gone in `customHttp.yml`); checkout custom domain resolves.

**Skipped / deferred (with reason) — the honest tail:**
- 22 — JSON-LD price/tier de-dup: the `@graph` re-encodes UI facts; a true single-source refactor
  is High-effort and runtime-touching. Open.
- 22 — homepage-head snapshot baseline: the drift *risk* is now covered by a test (#25); the
  snapshot-at-module-load refactor itself was not taken.
- 22 — `og:image:type` on issue cards still says `image/png` even for a Sanity JPG (alt is now
  correct, #25); MIME-hint only, very low impact.
- 23 — static-route `lastmod` still hardcoded: no clean derivation source; advisory only.
- 24 — build-time "no inline executable script" HTML scan: not implemented.
- 25 — single-source the Node pin / 3-command gate; Amplify `npm ci` cache + double-run: minor,
  not pursued.
- 26 — bats/extracted-helper tests for the bash script: bats not in the toolchain.
- 21 — injectHead sequential-pass / per-issue `mkdir` perf; GROQ projection de-dup (the two GROQ
  queries genuinely differ across packages). The `SITE` base de-dup IS done (`src/lib/site.js`).

### 21. Prerender + SSR pipeline
- [x] **[Security]** Escape CMS-sourced head values before injecting into static HTML — issue titles/descriptions/og:image flow verbatim into `<title>` and `content="..."` attributes; an ordinary editorial `"` or `&` corrupts the og/twitter/meta cards, and `</title><script>` ships into served HTML (CSP blocks execution but not card corruption / meta-refresh / injected forms). Add an `escapeHtml` helper, escape every interpolated value, and compare the post-swap guard against the escaped title. Impact: High. Effort: Low. (`scripts/lib/inject-head.mjs:21-39`, `scripts/prerender.mjs:79-85,123`) — added 2026-06-27; completed 2026-06-27 (#18)
- [x] **[Resilience]** Guard every injected head field, not just `<title>` — the post-injection assertion checks only the title, so on any index.html formatting drift the description/canonical/og:image/og:type/robots swaps silently no-op; a `noindex` issue would ship indexable and every issue could inherit the homepage canonical, with a green build. Have `injectHead` report which keys it actually swapped and assert each requested field changed. Impact: High. Effort: Low. (`scripts/prerender.mjs:122-125`, `scripts/lib/inject-head.mjs:32-48`) — added 2026-06-27; completed 2026-06-27 (#18 — injectHead now throws on any missed swap)
- [x] **[Observability]** Fail loudly when a production build has no Sanity creds or yields zero issues — `fetch-issues` treats a missing `SANITY_PROJECT_ID` as the intended preview path (writes `[]`, exits 0), so a prod build that lost its env var silently delists the entire newsletter archive (sitemap collapses to 4 routes) with a green deploy, indistinguishable from a dev machine. Gate the inert path on an explicit preview/CI flag, or emit a distinct warning + non-zero exit when zero issues are prerendered. Impact: Medium. Effort: Medium. (`scripts/fetch-issues.mjs:58-63`, `scripts/prerender.mjs:78,162`) — added 2026-06-27; completed 2026-06-27 (#18 — gated on `GSS_ALLOW_EMPTY_ISSUES`)
- [ ] **[Architecture]** Single source of truth for per-route head metadata — each indexable route's title/description is encoded twice (prerender `ROUTES` vs the page's `usePageMeta()`), and `/story` has already drifted (the prerendered description carries a "Christian Perez…" clause the hydrated hook drops, so non-JS crawlers and JS clients see different descriptions for the same URL). Extract one shared route-metadata module both consume. Impact: Medium. Effort: Medium. (`scripts/prerender.mjs:38`, `src/pages/Story.jsx:9`, `src/lib/usePageMeta.js:40-46`) — added 2026-06-27 — PARTIAL: homepage-head drift is now test-guarded (#25); the per-route shared module is still open
- [x] **[Security]** Validate the issue slug before using it as a filesystem path — the raw CMS slug is interpolated into `signal/${slug}.html` and `writeFile`'d; the `^[a-z0-9-]+$` rule lives only in the Studio (advisory, bypassable via API writes). Re-validate in `normalizeIssues`. Impact: Low. Effort: Low. (`scripts/prerender.mjs:90,128-130`, `scripts/fetch-issues.mjs:38-39`) — added 2026-06-27; completed 2026-06-27 (#18)
- [x] **[Cleaner]** Collapse `injectHead`'s repeated capture-group replace lambda (9-11 near-identical blocks) into a table-driven loop over `{regex, value}`. Impact: Low. Effort: Low. (`scripts/lib/inject-head.mjs:21-48`) — added 2026-06-27; completed 2026-06-27 (#18)
- [ ] **[Performance]** `injectHead` makes up to 12 sequential full-string `replace` passes (each copies the whole HTML), and the route loop re-`mkdir`s per issue and serializes all file I/O — batch the replaces / hoist the mkdir / parallelize writes. Impact: Low. Effort: Medium. (`scripts/lib/inject-head.mjs:17-50`, `scripts/prerender.mjs:107-131`) — added 2026-06-27 — deferred (Low impact; build-time only)
- [ ] **[Architecture]** De-duplicate the published-issue GROQ projection shared between the prerender pipeline and the broadcast backend, and the `SITE` base + route list duplicated across the build script, the client hook, and index.html. Impact: Low. Effort: Medium. (`scripts/fetch-issues.mjs:15-34`, `backend/broadcast/src/issue-email.mjs`) — added 2026-06-27 — PARTIAL: `SITE` base de-dup done (#25, `src/lib/site.js`); GROQ de-dup skipped (the two queries genuinely differ across packages)

### 22. Structured data & social meta (JSON-LD @graph)
- [x] **[Resilience]** Add a build/test guard that extracts the inline `ld+json` block, `JSON.parse`s it, and asserts every `@id` reference resolves to a node — the ~88-line hand-maintained `@graph` with internal cross-refs is never parse-checked, so one trailing comma or dangling `@id` makes Google silently discard all structured data with a green build. Impact: Medium. Effort: Low. (`index.html:29-117`, `scripts/prerender.mjs:100-125`) — added 2026-06-27; completed 2026-06-27 (#18 — `scripts/check-jsonld.mjs`)
- [ ] **[Architecture]** Make index.html the single source of truth for the homepage head — title/OG/description/robots are re-declared as JS constants in `usePageMeta.js` and unconditionally re-written over the static markup on hydration, so any index.html edit not mirrored in the constants visibly degrades the homepage after React boots. Snapshot the server-rendered head once at module load and use that as the reset baseline. Impact: Medium. Effort: Medium. (`src/lib/usePageMeta.js:5-17,56-61`, `index.html:6,12-13,24-25,28`) — added 2026-06-27 — drift risk now covered by a test (#25); the snapshot-at-module-load refactor itself was not taken
- [ ] **[Accessibility]** When `injectHead` swaps `og:image` on issue pages, update its companion `og:image:alt` and `og:image:type` — both still describe the homepage particle PNG, so a Signal issue's social card carries a mismatched alt/MIME for its own image. Impact: Low. Effort: Low. (`scripts/lib/inject-head.mjs:37-44`, `index.html:22,27`) — added 2026-06-27 — PARTIAL: `og:image:alt` now swapped (#25); `og:image:type` still `image/png` for JPG cards (MIME hint only, very low impact)
- [ ] **[Resilience]** An issue with no excerpt/SEO description ships the homepage meta description on its article page (disagreeing with the client fallback), and `usePageMeta` silently no-ops when a meta tag is absent (untested, inconsistent with its self-healing robots branch). Impact: Low. Effort: Low. (`scripts/prerender.mjs:81-82`, `src/lib/usePageMeta.js:19-27`) — added 2026-06-27
- [x] **[Cleaner]** Remove the redundant `ogTitle` ternary (always equals `fullTitle`) and the unreachable raw-DOM robots branch in `usePageMeta`. Impact: Low. Effort: Low. (`src/lib/usePageMeta.js:56`) — added 2026-06-27; completed 2026-06-27 (#18 — `ogTitle` ternary removed)
- [ ] **[Architecture]** The JSON-LD `@graph` re-encodes UI facts (prices, tier copy, org description) as a parallel source of truth that can drift from the rendered pages. Impact: Low. Effort: High. (`index.html:78-112`) — added 2026-06-27 — deferred (High-effort, runtime-touching; its own PR)

### 23. Sitemap & robots
- [x] **[Security]** XML-escape `<loc>`/`<lastmod>` and validate the CMS slug (`^[a-z0-9-]+$`) before it reaches the sitemap — an `&` or `<` in a slug emits invalid XML that makes crawlers reject the *entire* sitemap (every URL goes dark); the same value also feeds the output file path. Validate in `normalizeIssues` and add a shared escape helper at emit. Impact: Medium. Effort: Low. (`scripts/prerender.mjs:142-157`, `scripts/fetch-issues.mjs:38-39`) — added 2026-06-27; completed 2026-06-27 (#18)
- [x] **[Resilience]** Extract sitemap-entry construction (issues → entries) into a small pure, exported function and test it — the noIndex filter, slug→loc mapping, `publishedAt` slice, and null-lastmod path run only at build time, untested, while sibling pipeline pieces have tests. Impact: Medium. Effort: Medium. (`scripts/prerender.mjs:137-162`) — added 2026-06-27; completed 2026-06-27 (#18 — `scripts/lib/sitemap.mjs` + tests)
- [x] **[Cleaner]** Derive the sitemap from `ROUTES` instead of re-listing the static URLs and re-implementing the noIndex filter — indexable URLs and the indexability predicate are each declared twice in one file, so adding/renaming a route silently desyncs the sitemap. Attach sitemap meta + an `indexable` flag to the route records. Impact: Medium. Effort: Medium. (`scripts/prerender.mjs:30-89` vs `138-149`) — added 2026-06-27; completed 2026-06-27 (#18)
- [ ] **[Resilience]** Give the sitemap write the same build-time guard every other prerender step has, and derive static-route `lastmod` instead of hardcoding date literals that go stale silently. Impact: Low. Effort: Medium. (`scripts/prerender.mjs:138-141,161`) — added 2026-06-27 — PARTIAL: write guard shipped (#18, the "missing homepage URL" throw); static-route `lastmod` derivation skipped (no clean source, advisory)
- [x] **[Cleaner]** Stop committing `public/sitemap.xml` — it is always overwritten by the build and is already a stale, divergent second source of truth. Impact: Low. Effort: Low. (`public/sitemap.xml`) — added 2026-06-27; completed 2026-06-27 (#18 — deleted)
- [x] **[Resilience]** `robots.txt`'s `Disallow` list is hand-maintained and can drift from the actual noindex routes. Impact: Low. Effort: Low. (`public/robots.txt`) — added 2026-06-27; completed 2026-06-27 (#25 — sync note added; advisory)

### 24. Security headers + CSP
- [x] **[Consistency]** Add `https://cdn.sanity.io` to `img-src` — the enforcing CSP locks `img-src` to `'self' data:`, but Signal issue bodies render inline `<img>` straight from `cdn.sanity.io`, so the first published issue with a figure has its image silently blocked on the live, prerendered `/signal/:slug` route. (The "zero violations" soak never exercised the issue routes.) Also correct the "no third-party requests" comment. Impact: High. Effort: Low. (`customHttp.yml:16-24`, `src/components/IssueBody.jsx:26`, `scripts/fetch-issues.mjs:30`) — added 2026-06-27; completed 2026-06-27 (#21)
- [x] **[Security]** Pin `connect-src` to the exact checkout host instead of the region-wide `*.execute-api.us-east-2.amazonaws.com` wildcard, which lets the page reach any API Gateway in the region (incl. attacker-owned accounts) — defeating least-privilege for the checkout origin. Source it from `VITE_CHECKOUT_ENDPOINT` or front the API with a custom subdomain. Impact: Medium. Effort: Low. (`customHttp.yml:24`, `backend/checkout/template.yaml`) — added 2026-06-27; completed 2026-06-27 (#23 IaC + #24 — custom domain `checkout.groundstatesociety.com`, wildcard removed, verified live)
- [x] **[Resilience]** Reconcile `connect-src` with the configured `VITE_*_ENDPOINT` origins via a build/test guard — the policy is hand-synced to the endpoints and the imminent `/apply` paid-funnel origin is guarded only by a prose comment; a mismatch silently blocks the fetch and looks like a transient network error. Impact: Medium. Effort: Medium. (`customHttp.yml:21-24`, `docs/PROJECT_STATUS.md:60-62`, `src/pages/Apply.jsx:75-88`) — added 2026-06-27; completed 2026-06-27 (#21 — `scripts/check-csp.mjs`)
- [x] **[Resilience]** Extend `verify-deploy.sh` to assert the security headers (HSTS, nosniff, the CSP and its `connect-src` origins) are actually served — the script exists to catch silent config degradation but never checks a single header, and this account has a known "console config doesn't auto-apply" gotcha. Impact: Medium. Effort: Low. (`scripts/verify-deploy.sh:16-49`, `customHttp.yml:4-24`) — added 2026-06-27; completed 2026-06-27 (#20)
- [ ] **[Resilience]** Pin the "no inline executable script" invariant that `script-src 'self'` (no nonce/hash) silently relies on — add a build-time scan of every emitted `*.html` for inline `<script>`/`on*` handlers so a future bundling change that would white-screen production fails in CI, not post-deploy. Impact: Medium. Effort: Low. (`customHttp.yml:23-24`) — added 2026-06-27 — not implemented
- [x] **[Security]** Add `form-action 'self'` (and consider `frame-src 'none'`) — the CSP omits `form-action`, so an injected `<form>` could POST anywhere. Impact: Low. Effort: Low. (`customHttp.yml:24`) — added 2026-06-27; completed 2026-06-27 (#21 — `form-action 'self'`)

### 25. CI / deploy test gate
- [x] **[Resilience]** Wire the orphaned build-script tests into the gate — `scripts/lib/inject-head.test.mjs` (6 tests) and `scripts/fetch-issues.test.mjs` (2 tests) pass but execute in *no* gate: `npm test` globs only `backend/`, vitest includes only `src/**/*.{js,jsx}`, and CI/Amplify run only those. The prerender/head-injection/issue-normalization logic that produces production HTML ships unguarded behind a green suite. Switch to a recursive glob (`backend/**/test/*.test.mjs` + `scripts/**/*.test.mjs`). This single issue was independently surfaced by 8 of the 29 reviews. Impact: High. Effort: Low. (`package.json:11`, `vite.config.js:79`, `.github/workflows/ci.yml:19-21`) — added 2026-06-27; completed 2026-06-27 (#18)
- [x] **[Observability]** Make the backend gate fail on zero matched test files — `node --test` with a glob that matches nothing exits 0 ("tests 0 / pass 0 / fail 0"), so renaming/moving a backend test dir silently voids the deploy-blocking gate while staying green (vitest already exits 1 on no files). Impact: Medium. Effort: Low. (`package.json:11`, `amplify.yml:8-10`) — added 2026-06-27; completed 2026-06-27 (#18 — `scripts/check-test-globs.mjs` pretest)
- [x] **[Resilience]** Run `npm run build` in `ci.yml` (after lint) — the PR gate never builds, so the prerender `expect`-marker and head-injection guards (the "a silently-broken render can't ship" safety net) only fire at deploy, post-merge; a build-breaking PR goes green. The build runs cleanly without secrets (`fetch-issues` degrades to `[]`). Impact: Medium. Effort: Low. (`.github/workflows/ci.yml:18-21`, `amplify.yml:15-17`, `scripts/fetch-issues.mjs:58-63`) — added 2026-06-27; completed 2026-06-27 (#22)
- [x] **[Security]** Harden the GitHub Actions workflow — add an explicit least-privilege `permissions:` block (default token is often read/write for a test-only job), pin third-party actions by commit SHA instead of floating major tags, and add `timeout-minutes` + a `concurrency` group. Impact: Low. Effort: Low. (`.github/workflows/ci.yml`) — added 2026-06-27; completed 2026-06-27 (#22)
- [ ] **[Cleaner]** Single-source the Node 22 pin and the three-command test/lint gate that is copy-pasted verbatim across `amplify.yml` and `ci.yml`. Impact: Low. Effort: Low. (`amplify.yml`, `.github/workflows/ci.yml`) — added 2026-06-27 — not pursued (minor; the indirection wasn't judged worth it)
- [ ] **[Observability & cost]** Amplify caches `node_modules` but `npm ci` deletes it before reinstalling (every prod build re-downloads all deps), and the full suite+lint runs twice per merge (GitHub push CI + Amplify preBuild). Impact: Low. Effort: Low. (`amplify.yml`) — added 2026-06-27

### 26. Deploy verification script
- [x] **[Resilience]** Fix the bundle/endpoint check — it greps the entry `index-*.js` for the checkout host, but `VITE_CHECKOUT_ENDPOINT` is read only in the lazy-loaded `Activate`/`Welcome` chunks, so the host is *never* in the entry bundle and the check prints `FAIL: …VITE_CHECKOUT_ENDPOINT is stale or unset` on every healthy API-mode deploy (regression since the 2026-06-26 lazy-load refactor; the README says run it after every deploy). Enumerate all `/assets/*.js` (or the route chunk) and match with `grep -F` (the host is currently used as a regex, so its dots match any char). Impact: High. Effort: Medium. (`scripts/verify-deploy.sh:31,40`, `src/App.jsx:18-19`, `src/pages/Welcome.jsx:7`) — added 2026-06-27; completed 2026-06-27 (#20 — multi-chunk `grep -F` crawl)
- [x] **[Resilience]** The "SPA rewrite" probe verifies the wrong rule — it curls `/apply`, which matches the dedicated `/apply → /apply.html` prerender rewrite first, not the catch-all SPA fallback it claims to test (the most breakage-prone, hand-pasted rule). Probe a non-prerendered client route (e.g. `/welcome` or a random path) and assert the `data-route` marker instead of a generic `<div id="root"` grep; fix the stale "landing route" comment. Impact: Medium. Effort: Low. (`scripts/verify-deploy.sh:16,22-24`, `infra/amplify-rewrites.json`, `src/main.jsx:41`) — added 2026-06-27; completed 2026-06-27 (#20)
- [ ] **[Resilience]** Add tests for the script's pure logic (the https/localhost guard, trailing-slash normalization, host extraction, bundle-name regex, exit-code contract) — it is the production deploy gate yet has no tests, while its sibling build scripts do; tests would have caught the broken bundle check above. Impact: Medium. Effort: Medium. (`scripts/verify-deploy.sh:11-14,39`) — added 2026-06-27 — deferred (bats not in the toolchain; would need helper extraction)
- [x] **[Resilience]** Add `curl --max-time` and an explicit network-failure `FAIL` line — under `set -euo pipefail` with `-s`, a hung host hangs the script forever and a DNS/connection failure aborts with zero output instead of the script's own FAIL convention; also probe the sitemap the docs claim it checks. Impact: Low. Effort: Low. (`scripts/verify-deploy.sh:7,16,31`) — added 2026-06-27; completed 2026-06-27 (#20)
- [x] **[Security]** Tighten the cleartext guard — the `http://localhost*` glob over-matches remote hosts (e.g. `http://localhost.evil.com`), and the optional API base (arg 2) is curled with no scheme validation, unlike the site base. Impact: Low. Effort: Low. (`scripts/verify-deploy.sh:11-14`) — added 2026-06-27; completed 2026-06-27 (#20)
- [x] **[Cleaner]** De-duplicate the `/apply` double-fetch and the repeated full-flag `curl` idiom, and send `FAIL` diagnostics to stderr (not stdout) so they survive output redirection. Impact: Low. Effort: Low. (`scripts/verify-deploy.sh:16-26`) — added 2026-06-27; completed 2026-06-27 (#20)
