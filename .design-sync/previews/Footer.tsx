import { Footer } from 'ground-state-society-landing'

// The site footer: brand mark + wordmark, the page/join link columns, the
// decorative corner mosaic, and the membership coda. Self-contained; renders on
// the dark ground panel. (Router context is supplied globally via the preview
// provider, so the <Link>s resolve.)

export const Default = () => <Footer />
